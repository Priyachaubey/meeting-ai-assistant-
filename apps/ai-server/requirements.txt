# ── Core Framework ─────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.3
pydantic-settings==2.9.1
httpx==0.28.1
structlog==25.4.0
redis==6.2.0
websockets==12.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
aiosqlite==0.21.0
sqlalchemy[asyncio]==2.0.41
psycopg[binary]==3.2.9
alembic==1.16.1

# ── ML Engine: Speech (faster-whisper / CTranslate2) ───────────────────
faster-whisper==1.1.0

# ── ML Engine: LLM, Translation, Vision, Embedding (HuggingFace) ──────
torch>=2.2.0
transformers>=4.44.0
sentence-transformers>=3.0.0
accelerate>=0.30.0
safetensors>=0.4.0
tokenizers>=0.19.0

# ── ML Engine: Numerical ──────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0

# ── ML Engine: OCR ────────────────────────────────────────────────────
pytesseract>=0.3.10
Pillow>=10.0.0
# Optional PDF support: pip install pdf2image  OR  pip install pymupdf

# ── ML Engine: Audio ──────────────────────────────────────────────────
soundfile>=0.12.0
librosa>=0.10.0

# ── ML Engine: Optional GPU Quantization ──────────────────────────────
# bitsandbytes>=0.43.0  # 4-bit/8-bit quantization
# auto-gptq>=0.7.0      # GPTQ quantization

# ── ML Engine: Optional Speaker Diarization ────────────────────────────
# pyannote.audio>=3.1.0  # Requires HuggingFace token

# ── ML Engine: Optional TTS ────────────────────────────────────────────
# pyttsx3>=2.90          # Offline system TTS
# TTS>=0.22.0            # Coqui TTS (neural, higher quality)
